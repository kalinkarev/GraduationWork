<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/content_main"
    android:background="@color/colorBlueForEvents"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/medium_height"
    android:paddingLeft="@dimen/medium_height"
    android:paddingRight="@dimen/medium_height"
    android:paddingTop="@dimen/medium_height"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context="com.example.kalin.graduationwork.MainActivity"
    tools:showIn="@layout/activity_main">

    <ListView
        android:layout_width="match_parent"
        android:layout_height="match_parent"

        android:id="@+id/kalinsListView">

    </ListView>

</RelativeLayout>